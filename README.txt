Hoja de proyecto:

Se necesita en referencias del Visual Studio: Newtonsoft.Json

Se crea una clase que debe ajustarse segun lo necesitado,

El programa en cada accion que este relacionada a json, verifica antes si existe la carpeta o el archivo, en caso de que no, se crean en una ruta que se estime conveniente.

Se crea las funciones json de read y write que escriben o leen el archivo de la carpeta MyJSON

